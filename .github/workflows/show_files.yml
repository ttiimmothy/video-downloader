name: show_the_files
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-show-the-files:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Show the files 🔧
        run: |
          ls -al
